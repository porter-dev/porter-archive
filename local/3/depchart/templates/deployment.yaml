apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.MyName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: {{ .Values.MyName }}
        image: busybox
        command: ['sh', '-c', 'echo Hello Kubernetes! && sleep 3000']
